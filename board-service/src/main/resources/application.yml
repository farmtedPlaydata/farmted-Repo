spring:
  application:
    name: board-service
  datasource:
    url: jdbc:mysql://localhost:3306/msa_db
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true